# Reuse Delta

Delivered to portfolio-reuse-kit PR #4:

- V2 primary metric lookup in project validation
- Git tracked/non-ignored file scanning instead of cache traversal
- documented 400 responses for operational commands
- contract-set version 1.1.0 and regenerated integrity manifest

Recorded backlog: confirm compiled Nest decorator-metadata guidance in another Node repository before changing the shared skill.
